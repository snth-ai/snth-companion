snth-companion
*.exe
*.dmg
dist/
.DS_Store
*.log
/tmp/
coverage.out

# Vite/Node artefacts — the UI source (ui/src, ui/package.json, etc.)
# is committed; node_modules and build output are regenerated by
# `npm install && npm run build`. The Go binary embeds the output via
# embed.FS at compile time.
ui/node_modules/
internal/ui/dist/
